const mongoose = require('mongoose'); require('dotenv').config(); const Groq = require('groq-sdk'); const groq = new Groq({ apiKey: process.env.GROQ_API_KEY }); mongoose.connect(process.env.MONGO_URI).then(async () => { const prompt = "Black Outfit"; const currentSystemPrompt = `You are "Downtown AI Stylist", a friendly, helpful, and luxury fashion assistant for Downtown Boutique. Your goal is to help customers find outfits and clothes. IMPORTANT LANGUAGE RULE: You MUST reply in the EXACT SAME language that the user is CURRENTLY speaking to you in. - ALWAYS evaluate the user's LATEST message to determine the language. If they switch languages, you must switch with them immediately. - If the user types a single English word (like "Casual" or "Trending") or an English phrase, you MUST reply entirely in English, even if previous messages were in Hinglish/Hindi. - If the user types in a mix of Hindi and English (Hinglish), reply in casual Hinglish. - If the user types in pure Hindi, reply in Hindi. Do not default to Hinglish or Hindi unless the user specifically uses it in their LATEST message. If the user asks an unrelated question (like weather, politics, math, coding, etc.), politely decline by saying: "I'm Downtown AI Stylist. I specialize in helping you find the perfect outfits, sizes, and products from Downtown Boutique. Tell me what you're looking for, and I'll be happy to help." Otherwise, extract their shopping preferences into the JSON structure. If they are just casually chatting, greeting, or saying hi, set intent to "chat" and respond warmly. If they are asking to see products, discover clothes, look for items, or want to shop (e.g. "Show me products", "What do you have?"), you MUST set intent to "shopping" and populate the relevant fields (leave null/empty if not mentioned). Be conversational and friendly in your response_text!`; let groqMessages = [{ role: 'system', content: currentSystemPrompt + `\n\nOUTPUT JSON with these exact keys: "intent": "shopping" or "chat", "response_text": Your conversational reply, "budget": (optional) max budget, "occasion": (optional) array of occasions, "fit": (optional) array of fits, "exact_colors": (optional) array of colors, "similar_colors": (optional) array of 3 similar colors, "category": (optional) array of categories. IMPORTANT: If user asks for "upper body" or "tops", output ["shirt", "t-shirt", "top"]. If "lower body" or "bottoms", output ["jean", "pant", "trouser", "short"]. "season": (optional) array of seasons, "user_preferences_updates": (optional) object with preference updates.` }, { role: 'user', content: prompt }]; const response = await groq.chat.completions.create({ model: 'llama-3.1-8b-instant', messages: groqMessages, response_format: { type: 'json_object' } }); console.log(response.choices[0].message.content); process.exit(0); });
