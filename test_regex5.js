const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGO_URI).then(async () => { const Product = require('./models/Product'); const docs1 = await Product.find({ colors: /black/i }); const docs2 = await Product.find({ colors: /\\bblack\\b/i }); console.log('Found docs1:', docs1.length); console.log('Found docs2:', docs2.length); process.exit(0); });
